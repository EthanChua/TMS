app.get('/', (req, res) => {
  res.send('GET request to the homepage')
})
app.use
app.post

const p = new Promise ((resolve, reject)=> {
  if () {
    resolve('')
  } else {
    reject('')
  }
})
p.then((message)=> { // if resolve
  console.log ('this is in the then ' + message)
}).catch((message) =>{
  console.log('this is in the catch'  + message)
}) // if reject

async function 